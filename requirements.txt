# ── Core ──────────────────────────────────────────────────────────────────────
opencv-python==4.8.1.78
numpy==1.24.3
Pillow==10.1.0

# ── Face detection ────────────────────────────────────────────────────────────
# BlazeFace via MediaPipe (recommended — fastest on Pi)
mediapipe==0.10.9

# YOLOv8-nano via Ultralytics (optional — heavier but more accurate)
ultralytics==8.1.0

# TFLite runtime for Pi (lighter than full TensorFlow)
# On Raspberry Pi install with:
#   pip install tflite-runtime   (pre-built wheel from Google)
# On x86 laptop:
#   pip install tflite-runtime   (or skip — ONNX/Ultralytics used instead)

# ── Face recognition (dlib-based) ─────────────────────────────────────────────
face-recognition==1.3.0
dlib==19.24.2

# ── Servo control (Raspberry Pi + PCA9685 only) ───────────────────────────────
adafruit-circuitpython-servokit==1.3.7
adafruit-circuitpython-pca9685==3.4.8

# ── Utilities ─────────────────────────────────────────────────────────────────
scipy==1.11.4
imutils==0.5.4

# ── Optional tiny LLM runtime (install only if you use Tiny LLM in GUI) ─────
# llama-cpp-python==0.2.90
